{
  "name": "@kernelindex/sdk",
  "private": true,
  "type": "module",
  "description": "Generated KernelIndex API client and local manifest tooling (§13.9), shared by the CLI and MCP. Never published before the contract is stable.",
  "exports": {
    ".": "./src/index.ts",
    "./manifest": "./src/manifest.ts"
  },
  "scripts": {
    "generate": "openapi-typescript openapi.json -o src/generated/api.d.ts && pnpm exec biome format --write src/generated/api.d.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "canonicalize": "^2.1.0",
    "openapi-fetch": "^0.15.1",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "openapi-typescript": "^7.10.1",
    "typescript": "6.0.3",
    "@types/node": "24.13.3",
    "vitest": "^4.1.10"
  }
}
